import { ColorOptions, SizeOptions } from "@uiTypes";
import { IncrementOf5Options } from "@uiTypes";

export type TextBackdropProps = {
  label: string;
  opacity?: IncrementOf5Options;
  color?: ColorOptions;
  size?: SizeOptions;
};
