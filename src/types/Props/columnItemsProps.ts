import { ColumnOptions } from "@uiTypes";

export type ColumnItemsProps = {
  columnItems?: {
    xs?: ColumnOptions;
    sm?: ColumnOptions;
    md?: ColumnOptions;
    lg?: ColumnOptions;
    xl?: ColumnOptions;
    xxl?: ColumnOptions;
  };
};
