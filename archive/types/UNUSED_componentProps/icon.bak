import {
  SizeOptions,
  BackgroundProps,
  ColorProps,
  MarginProps,
} from "@uiTypes";
import { IconType } from "react-icons";

export type IconComponentProps = BackgroundProps &
  ColorProps &
  MarginProps & {
    icon: IconType | string; // Use IconType for react-icons
    iconSize?: SizeOptions;
    rounded?: boolean;
    variant?: "solid" | "outline";
  };
